/*#include <stdio.h>
#include "../include/commands.h"

int main() {
    printf("Test de la fonction exit:\n");
    
    printf("Appel de  cmd_exit(0)...\n");
    cmd_exit(0);  // Le programme doit se terminer ici avec le code 0
    
    // Si le programme continue après l'appel à exit, c'est qu'il y a un problème
    printf("Erreur: cmd_exit() n'a pas arrêté le programme!\n");
    return 1;
}
*/